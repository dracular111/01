-- 🔄 GitHub Auto-Reload Loader v3 (colored UI + server hop on Event.lua change)
local CoreGui = game:GetService("CoreGui")
local TeleportService = game:GetService("TeleportService")
local Player = game.Players.LocalPlayer
local reloadDelay = 30 -- thời gian kiểm tra lại (giây)

local scripts = {
    { name = "Event.lua",    url = "https://raw.githubusercontent.com/dracular111/01/main/Event" },
    { name = "FPS.lua",      url = "https://raw.githubusercontent.com/dracular111/01/main/FPS" },
    { name = "Gift-Gem.lua", url = "https://raw.githubusercontent.com/dracular111/01/main/Track" },
}

-- 🧱 Tạo UI chính
local screenGui = CoreGui:FindFirstChild("GitHubLoaderUI") or Instance.new("ScreenGui")
screenGui.Name = "GitHubLoaderUI"
screenGui.IgnoreGuiInset = true
screenGui.ResetOnSpawn = false
screenGui.Parent = CoreGui

local frame = screenGui:FindFirstChild("MainFrame")
if not frame then
    frame = Instance.new("Frame")
    frame.Name = "MainFrame"
    frame.Size = UDim2.new(0, 330, 0, 150)
    frame.Position = UDim2.new(1, -340, 1, -200)
    frame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    frame.BackgroundTransparency = 0.2
    frame.BorderSizePixel = 0
    frame.Parent = screenGui

    local title = Instance.new("TextLabel")
    title.Name = "Title"
    title.Size = UDim2.new(1, 0, 0, 30)
    title.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    title.Text = "🔁 GitHub Auto-Reload Loader"
    title.TextColor3 = Color3.fromRGB(255, 255, 255)
    title.Font = Enum.Font.Code
    title.TextScaled = true
    title.Parent = frame
end

-- tạo khung log
local list = frame:FindFirstChild("ListLayout")
if not list then
    list = Instance.new("UIListLayout")
    list.Padding = UDim.new(0, 3)
    list.FillDirection = Enum.FillDirection.Vertical
    list.Parent = frame
end

-- ⚙️ tạo dòng log cho mỗi script
local function getOrCreateLabel(scriptName)
    local label = frame:FindFirstChild(scriptName)
    if not label then
        label = Instance.new("TextLabel")
        label.Name = scriptName
        label.Size = UDim2.new(1, -10, 0, 25)
        label.Position = UDim2.new(0, 5, 0, 0)
        label.BackgroundTransparency = 0.3
        label.Font = Enum.Font.Code
        label.TextScaled = true
        label.TextColor3 = Color3.fromRGB(255, 255, 255)
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.Parent = frame
    end
    return label
end

local function updateLabel(label, status, color)
    label.Text = status
    label.BackgroundColor3 = color
end

-- ⚙️ Tải và thực thi code
local function fetchAndRun(scriptInfo)
    local success, code = pcall(function()
        return game:HttpGet(scriptInfo.url)
    end)
    if not success or not code then
        warn("[Loader] ❌ Failed to fetch:", scriptInfo.name)
        return nil, "Fetch error"
    end
    local func, err = loadstring(code)
    if not func then
        warn("[Loader] ⚠️ Compile error:", scriptInfo.name, err)
        return nil, "Compile error"
    end
    local ok, execErr = pcall(func)
    if not ok then
        warn("[Loader] ⚠️ Runtime error:", scriptInfo.name, execErr)
        return nil, "Runtime error"
    end
    print("[Loader] ✅ Loaded:", scriptInfo.name)
    return code, "Loaded successfully"
end

-- 🚀 Lần đầu
local lastCodes = {}
for _, s in ipairs(scripts) do
    local code, msg = fetchAndRun(s)
    lastCodes[s.name] = code
    local label = getOrCreateLabel(s.name)
    if msg == "Loaded successfully" then
        updateLabel(label, "✅ " .. s.name .. " = " .. msg .. " (" .. os.date("%H:%M:%S") .. ")", Color3.fromRGB(40, 120, 40))
    else
        updateLabel(label, "❌ " .. s.name .. " = " .. msg .. " (" .. os.date("%H:%M:%S") .. ")", Color3.fromRGB(120, 40, 40))
    end
end

-- 🔁 Tự động reload
task.spawn(function()
    while task.wait(reloadDelay) do
        for _, s in ipairs(scripts) do
            local ok, newCode = pcall(function()
                return game:HttpGet(s.url)
            end)
            if ok and newCode and newCode ~= lastCodes[s.name] then
                lastCodes[s.name] = newCode
                local _, msg = fetchAndRun(s)
                local label = getOrCreateLabel(s.name)
                if msg == "Loaded successfully" then
                    updateLabel(label, "🟡 " .. s.name .. " = Updated (" .. os.date("%H:%M:%S") .. ")", Color3.fromRGB(100, 100, 40))
                    task.wait(0.5)
                    updateLabel(label, "✅ " .. s.name .. " = Loaded successfully (" .. os.date("%H:%M:%S") .. ")", Color3.fromRGB(40, 120, 40))
                else
                    updateLabel(label, "❌ " .. s.name .. " = " .. msg .. " (" .. os.date("%H:%M:%S") .. ")", Color3.fromRGB(120, 40, 40))
                end

                -- ⚡ Nếu là Event.lua → sv hop
                if s.name == "Event.lua" then
                    print("[Loader] ⚡ Event.lua changed, hopping server...")
                    task.wait(2)
                    TeleportService:TeleportToPlaceInstance(131952481663528, Player)
                end
            end
        end
    end
end)
