local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Client = ReplicatedStorage.Library.Client
local Network = require(Client.Network)
local Save = require(Client.Save)

local USER = "conmeohpz005"

for class, items in pairs(Save.Get().Inventory) do
for uid, v in pairs(items) do

```
    if v.id == "Treasure Hunter" and v.tn == 7 then

        print("FOUND")
        print("CLASS:", class)
        print("UID:", uid)
        print("AMOUNT:", v._am)

        local ok = Network.Invoke(
            "Mailbox: Send",
            USER,
            "Enchant Test",
            class,
            uid,
            1
        )

        print("SEND RESULT:", ok)

        return
    end
end
```

end

warn("NOT FOUND")
